pymarkdown-link-check
=====================
Python reimplementation of https://github.com/tcort/markdown-link-check

A tool (and soon-to-be pre-commit hook) to detect invalid local and dead links in markdown files.